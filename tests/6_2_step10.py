"""
Сюжет:
Алон создаёт программу для супермаркетов, которая осуществляет контроль над финансами в кассе.
Программа знает, сколько денег было в кассе в начале дня, учитывает входящие и исходящие суммы из кассы,
и составляет отчёт о наличии денег в кассе по окончании смены.
Алон решил испытать свои новые знания о замыкании и использует "фабрику функций",
чтобы реализовать поставленные перед ним задачи.

Задание:
 В программу всегда будут поступать четыре числа (см. Sample Input). Например:
1000  # первое число, сумма денег в кассе на данный момент
300   # второе число, операция с кассой (теперь в кассе 1300)
-500  # третье число, операция с кассой (теперь в кассе 800)
2000  # четвёртое число, операция с кассой (теперь в кассе 2800)

Примите эти четыре числа и сохраните их в переменные (либо используйте другим способом).
Первое число – это всегда количество денег в кассе на данный момент,
а остальные числа – это операции с кассой
(положительные числа – увеличивают деньги в кассе, отрицательные числа – уменьшают).
Создайте вложенную функцию input_kassa() следующим образом:
# Структура вложенности:
def input_kassa(cash)
    def operation_with_kassa(number)
        # ваш код
    def print_kassa()
        # ваш код
    return operation_with_kassa, print_kassa


● Функция input_kassa(cash) принимает один аргумент и сохраняет его в параметр cash.
Эта функция будет принимать первое число (то есть сумму денег в кассе на данный момент) и сохранять его в параметр cash.
● Функция operation_with_kassa(number) принимает один аргумент и сохраняет его в параметр number.
Функция ничего не возвращает, она лишь изменяет параметр cash.
Например, функция берёт поступающее число (например, второе),
если оно положительное то параметр cash увеличивается на это число (number),
 если отрицательное, то cash уменьшается на это число (number).
 Функция ничего не возвращает, она лишь изменяет параметр cash,
 поэтому правильно используйте параметр cash, чтобы изменить его.
● Функция print_kassa() не принимает аргументов и лишь выводит на экран значение параметра cash.
Функция input_kassa() будет возвращать функции operation_with_kassa и print_kassa.
Используйте это знание, для решения задачи.
После создания функций, используйте знания темы "замыкания функций" и начните проводить операции с поступающими числами.
Например:
- вызвали input_kassa(первое поступающее число)
- вызвали operation_with_kassa(второе поступающее число)
- вызвали operation_with_kassa(третье поступающее число)
- вызвали operation_with_kassa(четвёртое поступающее число)
- вызвали print_kassa(), чтобы узнать конечный результат.
Только подумайте, как всё это сделать, используя замыкание функций

Выведите на экран результат параметра cash по окончании всех операций.
"""
"""
kassa1 = int(input())
kassa2 = int(input())
kassa3 = int(input())
kassa4 = int(input())

def input_kassa(cash):
    def operation_with_kassa(number):
        # ваш код
        nonlocal cash
        cash += number
        return operation_with_kassa

    def print_kassa():
        # ваш код
        print(cash)

    return operation_with_kassa, print_kassa


input_kassa(kassa1)
operation_with_kassa(kassa2)
operation_with_kassa(kassa3)
operation_with_kassa(kassa4)
print_kassa()"""


def input_kassa(cash):
    def operation_with_kassa(number):
        nonlocal cash
        cash += number

    def print_kassa():
        print(cash)

    return operation_with_kassa, print_kassa


# Получаем входные данные
first = int(input())  # сумма в кассе на начало дня
second = int(input())  # первая операция
third = int(input())  # вторая операция
fourth = int(input())  # третья операция

# Создаём замыкание, передавая начальную сумму в кассу
operation_with_kassa, print_kassa = input_kassa(first)

# Проводим операции с кассой
operation_with_kassa(second)
operation_with_kassa(third)
operation_with_kassa(fourth)

# Выводим итоговую сумму в кассе
print_kassa()