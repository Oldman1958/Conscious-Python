Цель задания:
Давайте создадим новый файл с помощью кода и запишем в него данные из словаря, чтобы выглядело как в блокноте ниже:

(Увы... Картинка не вставляется...)

Задание
Всё задание займёт у вас 4 строки кода:

Создайте словарь data = {'велосипед': 10, 'самокат': 20, 'мопед': 30}.
Этот словарь мы будем использовать для записи данных в файл.
С помощью with и open() создайте файл с именем 'magazine.txt' в режиме записи 'w', кодировкой 'utf-8' и псевдонимом file.
Создайте цикл for по словарю data. Используйте метод .items(), чтобы получить доступ к ключу и значению словаря,
а в качестве переменных используйте key, value. Напомню как работать с методом:
# напоминашка:
for key, value in словарь.items():


Запишите данные в файл используя .write() и f-строку.
Посмотрите на скриншот из блокнота (см. "Цель задания") и с помощью f-строки создайте такую же запись.
Проверка
Запустите код в программе (PyCharm или др.) и должен появиться файл 'magazine.txt',
зайдите в него и проверьте, все ли строки разместились именно так, как надо.

Скопируйте ваш код и вставьте для решения задания
В качестве ответа, вставьте 4 строки кода.